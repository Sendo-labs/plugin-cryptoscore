name: Auto Release

on:
  push:
    branches:
      - main  # ou master 
      - feat/workflows

jobs:
  release:
    uses: sendo-labs/reusable-workflows/.github/workflows/release.yml@v1
    with:
      release-type: node
    permissions:
      contents: write
      pull-requests: write